import classNames from 'classnames'
import { useStyles } from './styles'

const Home = () => {
    const classes = useStyles();

    return (
        <main>
            
        </main>
    );
};

export default Home;